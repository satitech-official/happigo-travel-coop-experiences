import Image from 'next/image'; import SiteShell from '../../components/site-shell'; import PageIntro from '../../components/page-intro'; import { destinations } from '../../data/site-config'; import ContactSection from '../../components/contact-section';
export const metadata = { title: 'Destinations', description: 'Discover group-friendly Indian destinations with Happigo.' };
export default function DestinationsPage() { return <SiteShell><PageIntro eyebrow="THE MAP IS JUST THE BEGINNING" title={<>Find your next<br/><i>good story.</i></>} text="India holds a thousand ways to gather. We help you choose the one that feels right for your people." cta="Ask about a destination"/><section className="destination-list section"><div className="content-wrap">{destinations.map((place, i) => <article className="destination-row" key={place.name}><div className="destination-row__count">0{i + 1}</div><div className="destination-row__image"><Image src={place.image} alt={`${place.name}, ${place.region}`} fill sizes="(max-width: 700px) 85vw, 40vw"/></div><div><p>{place.region}</p><h2>{place.name}</h2><span>Explore this route ↗</span></div></article>)}</div></section><ContactSection/></SiteShell>; }
