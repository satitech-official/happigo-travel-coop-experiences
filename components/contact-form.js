'use client';
import { useEffect, useState } from 'react';
import { LoaderCircle, Send, CheckCircle2 } from 'lucide-react';
const initial = { name: '', organization: '', phone: '', email: '', eventType: '', participants: '', location: '', date: '', budget: '', message: '' };
const labels = { name: 'Your name', organization: 'Organization / cooperative name', phone: 'Phone number', email: 'Email address', eventType: 'What are you planning?', participants: 'Number of participants', location: 'Preferred location', date: 'Tentative date', budget: 'Estimated budget', message: 'Tell us a little more' };
export default function ContactForm() { const [form, setForm] = useState(initial); const [errors, setErrors] = useState({}); const [status, setStatus] = useState('idle'); useEffect(() => { const interest = new URLSearchParams(window.location.search).get('interest'); if (interest) setForm((current) => ({ ...current, eventType: 'Custom Experience', message: `I would like to know more about ${interest}.` })); }, []); const update = (event) => setForm({ ...form, [event.target.name]: event.target.value }); const submit = (event) => { event.preventDefault(); const next = {}; ['name', 'phone', 'email', 'eventType', 'participants'].forEach((key) => { if (!form[key].trim()) next[key] = 'Please complete this field.'; }); if (form.email && !/^\S+@\S+\.\S+$/.test(form.email)) next.email = 'Enter a valid email address.'; if (Object.keys(next).length) { setErrors(next); return; } setErrors({}); setStatus('loading'); setTimeout(() => { setStatus('success'); setForm(initial); }, 850); };
  if (status === 'success') return <div className="form-success" role="status"><CheckCircle2 size={45}/><h3>Your note is on its way.</h3><p>Thank you. The Happigo team will be in touch soon to begin the conversation.</p><button className="button button--green" onClick={() => setStatus('idle')}>Send another enquiry</button></div>;
  return <form className="enquiry-form" noValidate onSubmit={submit}>{Object.keys(labels).map((key) => <label className={key === 'message' ? 'form-full' : ''} key={key}>{labels[key]}{key === 'eventType' ? <select name={key} value={form[key]} onChange={update} aria-invalid={Boolean(errors[key])}><option value="">Choose one</option><option>Co-op Event</option><option>Corporate Offsite</option><option>Group Tour</option><option>Custom Experience</option></select> : key === 'message' ? <textarea name={key} value={form[key]} onChange={update} rows="4" placeholder="A few details help us make the first reply useful."/> : <input type={key === 'email' ? 'email' : key === 'date' ? 'date' : key === 'participants' ? 'number' : 'text'} name={key} value={form[key]} onChange={update} aria-invalid={Boolean(errors[key])}/>} {errors[key] && <small className="form-error">{errors[key]}</small>}</label>)}<button className="button button--yellow form-submit" disabled={status === 'loading'}>{status === 'loading' ? <><LoaderCircle className="spin" size={17}/> Sending...</> : <>Send enquiry <Send size={16}/></>}</button><p className="form-note">This demo form is client-side only. Connect it to your preferred inbox or CRM before launch.</p></form>; }
